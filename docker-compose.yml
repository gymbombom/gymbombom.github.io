version: "3.3"

services:
  blog:
    image: jekyll/jekyll:4.0.0
    command: jekyll serve --force_polling --drafts --livereload --trace -increament
    ports:
      - "4000:4000"
    volumes:
      - ".:/srv/jekyll"

